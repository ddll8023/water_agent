# 水库水质智能监测分析平台 项目结构文档

## 技术栈

| 类别       | 技术                       | 说明                                          |
| ---------- | -------------------------- | --------------------------------------------- |
| 后端框架   | FastAPI / Uvicorn          | 异步 REST API 服务，端口 3443                 |
| ORM        | SQLAlchemy 2.0 + aiomysql  | 异步连接池管理 user/role 数据模型             |
| 认证安全   | JWT + python-jose + bcrypt | Bearer Token 签发/验证，密码哈希，RBAC 装饰器 |
| 配置管理   | Pydantic Settings          | 从 .env 加载全局配置，提供单例配置对象        |
| 前端框架   | Vue 3.5 + Composition API  | SPA 应用，script setup 语法                   |
| UI 组件库  | Element Plus 2.9           | 表单/按钮/输入框/表格等，中文语言包           |
| CSS 方案   | Tailwind CSS 3.4 + PostCSS | 原子化 CSS，teal 主题色扩展                   |
| 状态管理   | Pinia 2.3                  | 认证状态与 Token 持久化                       |
| 路由       | Vue Router 4.5             | 路由懒加载，路由守卫鉴权                      |
| HTTP 请求  | Axios 1.7                  | 自动 Token 注入，统一响应拦截                 |
| 构建工具   | Vite 6.0                   | 开发代理 /api → localhost:3443，端口 3444    |
| 图标库     | @element-plus/icons-vue    | 全局注册所有图标组件                          |
| 数据库     | MySQL 8.0                  | 用户/角色业务数据存储                         |
| 缓存       | Redis 7.x（aioredis）      | 已接入——监测数据 24h 缓存（趋势+最新值）     |
| 实时推送   | WebSocket（FastAPI 内置）  | 已接入——预警事件实时推送到浏览器              |
| 图数据库   | Neo4j 5.x                  | 已接入——图谱初始化 + 全局概览查询            |
| 向量数据库 | Chroma                     | 已接入——知识库文档向量化持久化存储            |
| Embedding  | DashScopeEmbeddings        | 已接入——文档切片向量化，模型 text-embedding-v4|
| AI 编排    | LangChain + LangGraph      | 已接入——文档解析（PyPDFLoader）与文本切片；Collector Pipeline / Analyst ReAct Agent / M4 智能问答 ReAct Agent（工具探索循环 + 支持 doc_type 过滤的向量检索） |
| 异步缓存   | async-lru 2.3              | 已接入——Analyst Agent 工具函数查询缓存（64 条目） |
| 大模型     | DeepSeek-V4 / Qwen-Max     | API Key 已配置，规划中                        |
| 运行环境   | Python 3.12                | 项目要求最低版本                              |
| 包管理     | uv（Python）/ npm（Node）  | 后端使用 uv，前端使用 npm                     |

## 项目结构

```
水库智能监测Agent/
├── backend/
│   ├── .env                        # 环境变量：MySQL/Redis/Neo4j/JWT/LLM/Embedding 配置
│   ├── .python-version             # Python 3.12 版本声明
│   ├── pyproject.toml              # 项目元数据（name/version 占位）
│   ├── requirements.txt            # 20+ 项依赖清单
│   ├── uv.lock                     # uv 依赖锁定文件
│   ├── init_data.sql               # 数据库初始化 SQL（建表 + 种子数据）
│   └── app/
│       ├── main.py                 # FastAPI 入口：CORS、路由注册、启动建表
│       ├── core/
│       │   ├── config.py           # 全局配置：数据库/缓存/图库/JWT/LLM/Embedding/上传参数
│       │   ├── database.py         # MySQL 异步引擎与会话工厂
│       │   ├── neo4j.py            # Neo4j 图数据库驱动初始化
│       │   ├── redis.py            # Redis 异步客户端（监测数据 24h 缓存）
│       │   ├── chroma.py           # Chroma 向量库 LangChain 包装
│       │   ├── ws_manager.py       # WebSocket 连接管理器
│       │   ├── security.py         # JWT 签发/验证、当前用户、角色权限
│       │   └── config.py           # 全局配置（含 PATROL_ANALYSIS_INTERVAL_HOURS）
│       ├── pipelines/
│       │   ├── __init__.py
│       │   ├── collector.py        # Collector 采集管道：普通异步函数，10min 采集+规则预警
│       │   └── report_generator.py # Report Generator Pipeline：普通异步函数，日巡检/月度/季度报告自动生成+后台触发生成
│       ├── agent/
│       │   ├── analyst.py          # Analyst ReAct Agent：StateGraph + ToolNode，6h 趋势分析
│       │   ├── analyst_tools.py    # Analyst ReAct Agent 工具集（4 个 @tool 函数）
│   │   ├── chat_agent.py       # 智能问答 ReAct Agent：StateGraph + ToolNode + 全链思考流式，用户提问后自主规划检索路径
│       │   └── chat_tools.py       # 智能问答 ReAct Agent 工具集（4 个 @tool 函数）
│       ├── states/
│       │   ├── patrol.py           # PatrolStatus（采集管道执行状态）
│       │   ├── monitoring.py       # ProcessResult（监测数据处理产出统计 DTO）
│       │   ├── analysis.py         # AnalystStatus, AnalystState（趋势分析工作流状态）
│       │   ├── report.py           # ReportStatus, ReportType（报告生成状态与类型）
│       │   └── chat_agent.py       # ChatAgentState（智能问答 ReAct Agent 工作流状态）
│       ├── constants/
│       │   ├── monitoring.py       # 监测数据常量
│       │   ├── documents.py        # 文档上传常量（扩展名、大小限制）
│       │   └── chat_agent.py       # 智能问答 Agent 常量（递归限制、超时、滑动窗口）
│       ├── models/
│       │   ├── user.py             # 用户 ORM
│       │   ├── reservoir.py        # 水库 ORM
│       │   ├── role.py             # 角色 ORM
│       │   ├── station.py          # 监测站点 ORM
│       │   ├── indicator.py        # 监测指标 ORM（含五类标准限值）
│       │   ├── monitoring.py       # 监测记录 ORM
│       │   ├── alert.py            # 预警事件 ORM
│       │   ├── alert_rule.py       # 预警规则 ORM
│       │   ├── knowledge_document.py # 知识库文档 ORM
│       │   ├── chat_session.py     # 对话会话 ORM
│       │   ├── chat_message.py     # 对话消息 ORM（含 msg_meta 存储工具调用信息）
│       │   ├── patrol_log.py       # 巡检日志 ORM（已补 executed_at 索引）
│       │   ├── patrol_analysis.py  # AI 分析记录 ORM
│       │   └── report.py           # 报告 ORM（daily/monthly/quarterly/event）
│       ├── schemas/
│       │   ├── common.py           # ApiResponse/Pagination/ErrorCode 枚举
│       │   ├── response.py         # success()/error() 构造器
│       │   ├── auth.py / users.py / roles.py / reservoir.py
│       │   ├── stations.py / indicators.py / monitorings.py
│       │   ├── alerts.py / dashboard.py / alert_rules.py
│       │   ├── documents.py        # 文档上传/列表/详情响应模型
│       │   ├── chat.py             # 问答请求 Schema
│       │   ├── graph.py            # 图谱响应模型
│       │   ├── graph_admin.py      # 图谱实体管理 Schema
│       │   └── patrol_log.py       # 巡检日志 Schema
│       │   └── report.py           # 报告 Schema（Request/Response 类）
│       ├── routers/
│       │   ├── auth.py / users.py / roles.py / reservoir.py
│       │   ├── stations.py / indicators.py / monitoring.py
│       │   ├── dashboard.py / alerts.py / alert_rules.py
│       │   ├── documents.py        # 文档上传路由
│       │   ├── chat.py             # 智能问答 SSE 路由
│       │   ├── graph.py            # 图谱路由
│       │   ├── graph_admin.py      # 图谱实体管理路由
│       │   └── patrol_log.py       # 巡检日志路由
│       │   └── report.py           # 报告中心路由
│       ├── services/
│       │   ├── auth.py / users.py / roles.py / reservoir.py
│       │   ├── stations.py / indicators.py
│       │   ├── monitoring.py / dashboard.py / alerts.py / alert_rules.py
│       │   ├── documents.py        # 文档上传 + 异步解析/切片/向量化
│       │           ├── chat.py             # 智能问答（ReAct Agent 流式 SSE + 会话管理）
│       │   ├── graph.py            # 图谱查询服务
│       │   ├── graph_admin.py      # 图谱实体管理服务
│       │   └── patrol_log.py       # 巡检日志查询服务
│       │   └── report.py           # 报告查询与审核服务
│       ├── data/
│       │   └── graph_seed.json        # 图谱种子数据（河流/污染源/关系）
│       ├── neo4j_data/
│       │   └── neo4j-knowledge-graph.cypher  # Neo4j 调试脚本
│       ├── prompt/
│       │   ├── chat.yaml           # 对话模型提示词模板（旧，待清理）
│       │   ├── chat_agent.yaml     # 智能问答 Agent 提示词模板（SYSTEM/CHAT/SQL_GENERATION）
│       │   ├── alert.yaml          # 预警提示词模板（SUGGESTION + ANALYSIS + REACT_SYSTEM 章节）
│       │   └── report.yaml         # 报告生成提示词模板（DAILY/MONTHLY/QUARTERLY/EVENT 章节）
│       ├── utils/
│       │   ├── file.py             # 异步文件保存
│       │   ├── exception.py        # ServiceException
│       │   ├── logger_config.py    # 日志配置
│       │   ├── db_init.py          # 数据库初始化脚本
│       │   ├── model_factory.py    # LLM/Embedding 模型工厂单例
│   │   └── retriever.py        # RAG 检索工具（指定 doc_type 时走纯向量检索，不指定时走双路 RRF 融合；含 invalidate_retriever 失效标记）
│   └── test/
│       ├── test_patrol.py          # 采集/分析管道手动测试脚本
│       ├── test_report.py          # 报告生成手动测试脚本
│       └── test_rag_retrieval.py   # RAG 检索质量诊断测试
├── frontend/
│   ├── package.json                # Vue 3.5 + Element Plus + Tailwind + Axios + marked 等
│   ├── vite.config.js              # 端口 3444，/api → localhost:3443 代理
│   ├── tailwind.config.js          # teal 主题色、Inter 字体
│   ├── postcss.config.js           # tailwindcss + autoprefixer
│   └── src/
│       ├── main.js / App.vue / style.css
│       ├── api/                    # 17 个 API 模块（新增 report.js、agent.js、patrol_log.js）
│       ├── router/index.js         # 路由懒加载 + 守卫
│       ├── stores/auth.js          # Pinia 认证状态
│       ├── utils/request.js        # Axios 封装（Token/拦截/401）
│       ├── composables/useAlertWebSocket.js
│       └── views/
│           ├── layout/MainLayout.vue   # 侧边栏 + 顶部栏
│           ├── auth/ / dashboard/ / alert/ / monitoring/
│           ├── rag/KnowledgeBase.vue   # 知识库管理页
│           ├── rag/ChatIndex.vue       # 智能问答页
│           ├── rag/GraphView.vue       # 知识图谱可视化页面
│           ├── rag/graphConfig.js      # 图谱视觉配置
│           └── system/                 # 用户/水库/预警规则管理
```

## 依赖配置文件

### 依赖配置文件

| 文件                       | 说明                                                                         |
| -------------------------- | ---------------------------------------------------------------------------- |
| backend/pyproject.toml     | Python 项目元数据，name=backend/version=0.1.0，Python>=3.12；含 langgraph、langgraph-prebuilt、async-lru |
| backend/requirements.txt   | 20+ 项依赖：FastAPI、SQLAlchemy、aiomysql、redis、neo4j、LangChain、LangGraph、bcrypt、pypdf、python-docx、langchain-chroma、langchain-community、langchain-openai、rank-bm25、jieba、async-lru 等 |
| backend/.env               | MySQL/Redis/Neo4j/Chroma/JWT/DeepSeek/Qwen/Embedding 连接配置                |
| frontend/package.json      | Vue 3.5、Element Plus 2.9、Pinia 2.3、Axios 1.7、Tailwind 3.4、Vite 6.0      |
| frontend/vite.config.js    | 端口 3444，Vue 插件，@ 别名，/api → localhost:3443 代理                     |
| frontend/tailwind.config.js | 扫描范围、teal 主题色扩展、Inter 字体                                        |
| frontend/postcss.config.js | tailwindcss + autoprefixer 插件                                              |
| backend/.python-version    | Python 3.12                                                                  |


